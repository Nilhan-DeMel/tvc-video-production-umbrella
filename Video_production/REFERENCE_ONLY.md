# Reference Only

`Video_production/` is preserved here as historical reference code.

## Policy

- No live launching from this subtree
- No CI or routine test execution from this subtree
- No shortcuts or runtime paths may target this subtree
- Use it only to inspect older behavior, missing functions, or prior implementation ideas

The only live executable app in this umbrella repo is:

`..\Video_production_agent\`
